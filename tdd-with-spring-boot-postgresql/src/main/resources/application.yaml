logging:
  level:
    org:
      springframework: DEBUG

spring:
  application:
    name: TDD with Spring Boot and PostgreSql
  jpa:
    hibernate:
      ddl-auto: none
    properties:
      hibernate:
        default_schema: tdd
        show-sql: true
        format-sql: true
#  datasource:
#    url: jdbc:postgresql://localhost:5432/postgres
#    username: postgres
#    password: postgres
